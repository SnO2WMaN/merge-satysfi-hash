# deno-nixtemplate

## Usage

1. Use this template as _GitHub public template_.
1. `$ direnv allow`

### Commands

Default commands

- `nix run`
  - compile `./mod.ts` and run it.
- `update_lockfile`
  - update `lock.json` for caching.
